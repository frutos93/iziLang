PROGRAMA example2:

    FUNCION ENTERO factorial(ENTERO n){
        SI(n == 1){
            RETURN 1;
        };
        factorial(n-1);
     }

    MAIN()
    {
        ENTERO a;
        ENTERO b;
        a = factorial(2);
        IMPRIME(a);
    }

FIN